 IF message contains call AND
     IF message contains ll THEN
         type = ham
     IF message does not contains ll AND
         IF message contains mobile THEN
             type = spam
         IF message does not contains mobile AND
             IF message contains claim THEN
                 type = spam
             IF message does not contains claim AND
                 IF message contains 1 THEN
                     type = spam
                 IF message does not contains 1 AND
                     IF message contains landline THEN
                         type = spam
                     IF message does not contains landline AND
                         IF message contains text THEN
                             type = spam
                         IF message does not contains text AND
                             IF message contains account THEN
                                 type = spam
                             IF message does not contains account AND
                                 IF message contains uk THEN
                                     type = spam
                                 IF message does not contains uk AND
                                     IF message contains contact THEN
                                         type = spam
                                     IF message does not contains contact AND
                                         IF message contains xmas THEN
                                             type = spam
                                         IF message does not contains xmas AND
                                             IF message contains sorry THEN
                                                 type = spam
                                             IF message does not contains sorry AND
                                                 IF message contains 21 THEN
                                                     type = spam
                                                 IF message does not contains 21 THEN
                                                     type = ham
 IF message does not contains call AND
     IF message contains free AND
         IF message contains ü THEN
             type = ham
         IF message does not contains ü AND
             IF message contains aftr THEN
                 type = ham
             IF message does not contains aftr AND
                 IF message contains kb THEN
                     type = ham
                 IF message does not contains kb AND
                     IF message contains wan THEN
                         type = ham
                     IF message does not contains wan AND
                         IF message contains txt THEN
                             type = spam
                         IF message does not contains txt AND
                             IF message contains text THEN
                                 type = spam
                             IF message does not contains text AND
                                 IF message contains uk THEN
                                     type = spam
                                 IF message does not contains uk THEN
                                     type = spam
     IF message does not contains free AND
         IF message contains txt AND
             IF message contains doing THEN
                 type = ham
             IF message does not contains doing THEN
                 type = spam
         IF message does not contains txt AND
             IF message contains text AND
                 IF message contains time THEN
                     type = ham
                 IF message does not contains time AND
                     IF message contains phone THEN
                         type = ham
                     IF message does not contains phone AND
                         IF message contains hi THEN
                             type = ham
                         IF message does not contains hi AND
                             IF message contains miss THEN
                                 type = ham
                             IF message does not contains miss AND
                                 IF message contains shit THEN
                                     type = ham
                                 IF message does not contains shit THEN
                                     type = spam
             IF message does not contains text AND
                 IF message contains currently THEN
                     type = spam
                 IF message does not contains currently AND
                     IF message contains cost AND
                         IF message contains pobox THEN
                             type = spam
                         IF message does not contains pobox AND
                             IF message contains tone THEN
                                 type = spam
                             IF message does not contains tone THEN
                                 type = ham
                     IF message does not contains cost AND
                         IF message contains std THEN
                             type = spam
                         IF message does not contains std AND
                             IF message contains ringtone THEN
                                 type = spam
                             IF message does not contains ringtone AND
                                 IF message contains rate THEN
                                     type = spam
                                 IF message does not contains rate AND
                                     IF message contains station AND
                                         IF message contains gas THEN
                                             type = ham
                                         IF message does not contains gas THEN
                                             type = spam
                                     IF message does not contains station AND
                                         IF message contains lost AND
                                             IF message contains help THEN
                                                 type = spam
                                             IF message does not contains help THEN
                                                 type = ham
                                         IF message does not contains lost AND
                                             IF message contains girls AND
                                                 IF message contains waiting THEN
                                                     type = spam
                                                 IF message does not contains waiting THEN
                                                     type = ham
                                             IF message does not contains girls THEN
                                                 type = ham
